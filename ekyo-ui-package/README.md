# Package UI/UX Ekyo Marketing

Ce package contient tous les styles, animations et composants réutilisables du site ekyo-marketing.

## 📦 Contenu

- `globals.css` - Tous les styles CSS et animations
- `tailwind.config.ts` - Configuration Tailwind avec les couleurs
- `useScrollAnimation.ts` - Hook React pour animations au scroll
- `AnimatedSection.tsx` - Composant wrapper pour animations
- `EXAMPLES.md` - Exemples d'utilisation

## 🚀 Installation

1. Copiez `globals.css` dans votre dossier de styles globaux (ex: `src/app/globals.css`)
2. Copiez `tailwind.config.ts` ou fusionnez les couleurs dans votre config existante
3. Copiez les hooks dans `src/hooks/`
4. Copiez les composants dans `src/components/`

## 🎨 Utilisation

Voir `EXAMPLES.md` pour des exemples d'utilisation des patterns UI/UX.
